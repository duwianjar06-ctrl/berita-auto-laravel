@extends('layouts.app') @section('content')<h1 class="text-3xl font-bold">Instagram Perlu Perhatian</h1><div class="mt-6 space-y-4">@foreach($items as $item)<div class="rounded-xl bg-white p-5 ring-1 ring-rose-200"><div class="font-semibold">{{ $item->article?->title }}</div><div class="mt-2 text-sm text-rose-700">{{ $item->status }} — {{ $item->last_error }}</div></div>@endforeach</div><div class="mt-6">{{ $items->links() }}</div>@endsection
