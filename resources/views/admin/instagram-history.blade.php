@extends('layouts.app') @section('content')<h1 class="text-3xl font-bold">Instagram History</h1><div class="mt-6 space-y-3">@foreach($items as $item)<div class="rounded-xl bg-white p-5 ring-1 ring-slate-200"><div class="font-semibold">{{ $item->article?->title }}</div><div class="mt-1 text-sm text-slate-500">{{ optional($item->published_at)->format('d M Y H:i:s') }} · media {{ $item->media_id }}</div></div>@endforeach</div><div class="mt-6">{{ $items->links() }}</div>@endsection
