@extends('layouts.app') @section('content')<div class="flex items-center justify-between"><div><h1 class="text-3xl font-bold">Admin Instagram</h1><p class="mt-1 text-sm text-slate-500">Preparation dan publisher terpisah. Publisher Laravel tetap OFF secara default.</p></div><a class="underline" href="{{ route('admin.instagram.queue') }}">Queue</a></div><div class="mt-6 grid gap-4 md:grid-cols-3"><div class="rounded-xl bg-white p-5 ring-1 ring-slate-200"><div class="text-sm text-slate-500">READY</div><div class="mt-2 text-3xl font-bold">{{ $ready->count() }}</div></div><div class="rounded-xl bg-white p-5 ring-1 ring-slate-200"><div class="text-sm text-slate-500">Last preparation</div><div class="mt-2">{{ optional($latestRun?->completed_at)->format('d M Y H:i:s') ?: 'Belum ada' }}</div></div><div class="rounded-xl bg-white p-5 ring-1 ring-slate-200"><div class="text-sm text-slate-500">Publisher</div><div class="mt-2 font-semibold">{{ config('berita.instagram_auto_publish') ? 'ON' : 'OFF' }}</div></div></div><div class="mt-6 grid gap-4 md:grid-cols-3">@foreach($ready as $post)<div class="rounded-xl bg-white p-5 ring-1 ring-slate-200">@if($post->image_url)<img src="{{ $post->image_url }}" class="h-40 w-full rounded-lg object-cover">@endif<h2 class="mt-3 font-semibold">{{ $post->article->title }}</h2><p class="mt-2 text-sm text-slate-600">{{ $post->caption }}</p><span class="mt-3 inline-block rounded-full bg-emerald-100 px-2 py-1 text-xs text-emerald-700">READY</span></div>@endforeach</div>@endsection
