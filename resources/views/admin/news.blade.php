@extends('layouts.app')
@section('content')
<div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-xs font-bold uppercase tracking-widest text-slate-500">Ruang redaksi</p><h1 class="mt-1 text-3xl font-black tracking-tight">Admin Berita</h1><p class="mt-1 text-sm text-slate-500">Review, validasi, edit, jadwalkan, dan terbitkan draft dari content engine.</p></div><form method="post" action="{{ route('admin.logout') }}">@csrf<button class="rounded-lg border border-slate-200 bg-white px-4 py-2 text-sm font-semibold">Keluar</button></form></div>
<div class="mt-6 grid gap-4 sm:grid-cols-3"><div class="rounded-xl bg-white p-5 ring-1 ring-slate-200"><p class="text-xs font-bold uppercase text-slate-500">Total hasil</p><p class="mt-2 text-2xl font-black">{{ number_format($articles->total()) }}</p></div><div class="rounded-xl bg-white p-5 ring-1 ring-slate-200"><p class="text-xs font-bold uppercase text-slate-500">Sumber aktif</p><p class="mt-2 text-2xl font-black">{{ $sources->where('enabled',true)->count() }}</p></div><div class="rounded-xl bg-white p-5 ring-1 ring-slate-200"><p class="text-xs font-bold uppercase text-slate-500">Run terakhir</p><p class="mt-2 text-lg font-black">{{ optional($runs->first()?->finished_at ?? $runs->first()?->started_at)->format('d M H:i') ?: 'Belum ada' }}</p><p class="text-xs text-slate-500">{{ $runs->first()?->status ?: 'Belum tersedia' }}</p></div></div>
<section class="mt-8 rounded-2xl bg-white p-5 ring-1 ring-slate-200"><div class="flex items-center justify-between"><h2 class="font-extrabold">Antrian editorial</h2><span class="text-xs text-slate-500">AI tidak menerbitkan langsung</span></div><div class="mt-4 flex flex-wrap gap-2 text-xs font-bold"><a class="rounded-full bg-slate-100 px-3 py-2" href="{{ route('admin.news') }}">Semua</a>@foreach(['draft','review','scheduled','published','rejected'] as $status)<a class="rounded-full bg-slate-100 px-3 py-2 uppercase" href="{{ route('admin.news',['status'=>$status]) }}">{{ $status }}</a>@endforeach</div></section>
<form class="mt-5 grid gap-3 sm:grid-cols-[1fr_1fr_auto]"><input name="q" value="{{ request('q') }}" placeholder="Cari headline" class="rounded-lg border border-slate-200 bg-white p-3"><select name="source" class="rounded-lg border border-slate-200 bg-white p-3"><option value="">Semua sumber</option>@foreach($sources as $source)<option value="{{ $source->name }}" @selected(request('source')===$source->name)>{{ $source->name }}</option>@endforeach</select><button class="rounded-lg bg-slate-900 px-5 py-3 font-semibold text-white">Filter</button></form>
<div class="mt-5 space-y-3">@forelse($articles as $article)<article class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><div class="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between"><div class="min-w-0"><div class="flex flex-wrap gap-2 text-[11px] font-black uppercase tracking-wide"><span class="rounded-full bg-slate-100 px-2 py-1">{{ $article->generation_status }}</span><span class="rounded-full bg-slate-100 px-2 py-1">score {{ $article->quality_score ?? '—' }}</span><span class="px-2 py-1 text-slate-500">{{ $article->category?->name ?: 'Tanpa kategori' }}</span></div><h2 class="mt-2 text-xl font-black leading-snug"><a href="{{ route('article',$article->slug) }}" class="hover:underline">{{ $article->title }}</a></h2><p class="mt-2 text-sm text-slate-500">{{ $article->source_name ?: $article->publisher ?: 'Sumber tidak diketahui' }} · {{ optional($article->updated_at_content ?: $article->updated_at)->format('d M Y H:i') }}</p>@if($article->quality_reasons)<p class="mt-2 text-xs text-amber-700">Quality flags: {{ $article->quality_reasons }}</p>@endif</div><div class="flex flex-wrap gap-2 lg:max-w-sm lg:justify-end">@if(in_array($article->generation_status,['draft','review'],true))<a href="{{ route('admin.news.edit',$article) }}" class="rounded-lg border border-slate-300 px-3 py-2 text-xs font-bold">Edit</a><form method="post" action="{{ route('admin.news.approve',$article) }}">@csrf<button class="rounded-lg bg-emerald-700 px-3 py-2 text-xs font-bold text-white">Approve</button></form><form method="post" action="{{ route('admin.news.publish',$article) }}">@csrf<button class="rounded-lg bg-slate-900 px-3 py-2 text-xs font-bold text-white">Publish</button></form><form method="post" action="{{ route('admin.news.reject',$article) }}">@csrf<button class="rounded-lg border border-red-200 px-3 py-2 text-xs font-bold text-red-700">Reject</button></form>@endif</div></div></article>@empty<div class="rounded-2xl border border-dashed border-slate-300 bg-white p-8 text-center text-sm text-slate-500">Tidak ada artikel pada filter ini.</div>@endforelse</div><div class="mt-6">{{ $articles->links() }}</div>
<section class="mt-8 rounded-2xl bg-slate-950 p-5 text-white"><h2 class="font-extrabold">Automation status</h2><div class="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">@forelse($runs->take(6) as $run)<div class="rounded-lg border border-white/10 bg-white/5 p-4"><p class="text-xs uppercase tracking-wide text-slate-400">{{ $run->type ?: 'run' }}</p><p class="mt-1 font-bold">{{ $run->status ?: 'unknown' }}</p><p class="mt-1 text-xs text-slate-400">{{ optional($run->started_at)->format('d M Y H:i') }}</p></div>@empty<div class="text-sm text-slate-400">Belum ada run automation tercatat.</div>@endforelse</div></section>
@endsection
