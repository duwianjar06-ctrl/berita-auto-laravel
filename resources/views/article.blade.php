@extends('layouts.app') @section('content')<article class="mx-auto max-w-4xl rounded-xl bg-white p-6 shadow-sm ring-1 ring-slate-200"><div class="text-sm text-slate-500">{{ $article->category?->name }} · {{ optional($article->site_published_at)->format('d M Y H:i') }}</div><h1 class="mt-3 text-4xl font-bold leading-tight">{{ $article->title }}</h1>@if($article->image_url)<img src="{{ $article->image_url }}" alt="{{ $article->image_alt ?: $article->title }}" class="mt-6 max-h-[520px] w-full rounded-lg object-cover">@endif<div class="prose prose-slate mt-8 max-w-none">{!! nl2br(e($article->content)) !!}</div><div class="mt-8 border-t pt-4 text-sm text-slate-500">Sumber: {{ $article->source_name ?: $article->publisher }} · <a class="underline" href="{{ $article->source_url }}" rel="nofollow noopener" target="_blank">Baca sumber</a></div></article>@endsection
