@extends('layouts.app') @section('content')<h1 class="text-3xl font-bold">{{ $cat->name }}</h1><div class="mt-6 grid gap-5 md:grid-cols-3">@foreach($articles as $article)<article class="rounded-xl bg-white p-5 shadow-sm ring-1 ring-slate-200"><div class="text-xs text-slate-500">{{ optional($article->site_published_at)->format('d M Y H:i') }}</div><h2 class="mt-2 text-lg font-semibold"><a href="{{ route('article',$article->slug) }}">{{ $article->title }}</a></h2><p class="mt-2 text-sm text-slate-600">{{ $article->excerpt }}</p></article>@endforeach</div><div class="mt-8">{{ $articles->links() }}</div>@endsection
