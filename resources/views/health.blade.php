@extends('layouts.app') @section('content')<h1 class="text-3xl font-bold">System Health</h1><div class="mt-6 grid gap-4 md:grid-cols-4">@foreach(['Database'=>$db,'Automation'=>$automation?'ON':'OFF','Preparation'=>$preparation?'ON':'OFF','Publisher'=>$publisher?'ON':'OFF'] as $k=>$v)<div class="rounded-xl bg-white p-5 ring-1 ring-slate-200"><div class="text-sm text-slate-500">{{ $k }}</div><div class="mt-2 text-xl font-semibold">{{ $v }}</div></div>@endforeach</div><div class="mt-6 rounded-xl bg-white p-5 ring-1 ring-slate-200"><div>Last preparation: {{ optional($lastPreparation?->completed_at)->toDateTimeString() ?: '—' }}</div><div class="mt-2">Last publish: {{ optional($lastPublish?->published_at)->toDateTimeString() ?: '—' }}</div><div class="mt-2">Last run: {{ optional($lastRun?->finished_at)->toDateTimeString() ?: '—' }}</div></div>@endsection
