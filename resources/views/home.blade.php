@extends('layouts.app') @section('content')<div class="mb-8"><h1 class="text-3xl font-bold">Berita Terkini</h1><p class="mt-2 text-slate-600">Berita terbaru dari sumber resmi dan terpercaya.</p></div><div class="grid gap-5 md:grid-cols-3">@foreach($articles as $article)<article class="overflow-hidden rounded-xl bg-white shadow-sm ring-1 ring-slate-200">@if($article->image_url)<img src="{{ $article->image_url }}" alt="{{ $article->image_alt ?: $article->title }}" class="h-48 w-full object-cover">@endif<div class="p-5"><div class="text-xs text-slate-500">{{ $article->category?->name }} · {{ optional($article->site_published_at)->format('d M Y H:i') }}</div><h2 class="mt-2 text-lg font-semibold"><a href="{{ route('article',$article->slug) }}">{{ $article->title }}</a></h2><p class="mt-2 text-sm text-slate-600">{{ $article->excerpt }}</p></div></article>@endforeach</div><div class="mt-8">{{ $articles->links() }}</div>@endsection
